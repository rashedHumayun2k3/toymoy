export interface Product {
    id: string;         // Unique identifier for the toy
    name: string;       // Name of the toy
    description: string; // Description of the toy
    price: number;      // Price of the toy
    imageUrl: string;   // URL to an image of the toy
  }
  